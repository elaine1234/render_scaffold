{
  "name": "render-scaffold-lerna",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --force-local",
    "postinstall": "lerna bootstrap --force-local",
    "start": "lerna run start --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.4.3"
  }
}
